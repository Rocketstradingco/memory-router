FROM python:3.12-slim
WORKDIR /app
RUN pip install --no-cache-dir "mcp>=1.2,<2"
COPY mcp_server.py /app/
ENV MCP_HOST=0.0.0.0 MCP_PORT=8131 MR_BASE=http://memory-router:8130
EXPOSE 8131
CMD ["python", "mcp_server.py"]
